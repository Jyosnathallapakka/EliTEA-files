PASS

Created exactly one Playwright TypeScript test file from the approved manual test cases and saved/uploaded it to the repository:

- `tests/AuditLogging.spec.ts`

Summary:
- Added 5 `test()` cases, one for each approved manual test case:
  - rating submission audit log
  - user deactivation audit log
  - delegation change audit log
  - file upload audit log
  - approval action audit log
- Used production-style Playwright patterns with reusable helper functions.
- Included environment-variable-driven credentials and inputs to keep the script adaptable.

If you'd like, I can also create a Pull Request for this branch.